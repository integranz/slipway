# generated by slipway (stack dotnet8-api). Used by BuildKit for `docker build -f <% app.build.dockerfile %> <% app.build.context %>`
# (it takes precedence over the context's .dockerignore).
<%#if app.build.context_is_repo_root%>.git
.github
.slipway
.claude
infra
**/node_modules
**/dist
**/.env*
**/*.md
<%/if%>**/bin/
**/obj/
**/TestResults/
<% app.build.src_prefix %>tests/
