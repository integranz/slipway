# Command catalog (goal 3)

Five user-invoked skills (`disable-model-invocation: true`), each with arguments, defaults and a safety note. Invoke as `/slipway:<name>` once the plugin is installed. Status: **5 of 5 implemented and exercised on adlc-demo** (2026-09-14 → 2026-09-16). Since plugin 0.13.0 (2026-09-17) `deploy`, `verify` and `plan --layer apps/<app>` work per app (one CI/CD pair, module and version per app); the per-app arguments are exercised at the demo cutover.

| # | Command | Arguments | Defaults | Safety | Status | Example |
|---|---|---|---|---|---|---|
| 1 | `/slipway:dockerize` | `$0` app path or name; `--no-run`, `--compose`, `--force` | version from the configured versioning tool (`nbgv get-version -v SemVer2`), else `0.0.0-local`; local tag `<project>/<app>:<version>` | never pushes, never tags `latest`, never edits generated files, stops its containers, distroless runtime stays untouched | **implemented, exercised 2026-09-14** (`adlc-demo/api:0.1.6`) | `/slipway:dockerize apps/api` |
| 2 | `/slipway:plan` | `$0` env; `--layer foundation\|apps/<app>`; `--image-tag` (app module) | `dev`, `foundation`; `ARM_SUBSCRIPTION_ID` from `az account show` | plan only, never applies or formats in place; prints the human approval protocol; plan file gitignored and hook-protected | **implemented, exercised 2026-09-14** (`adlc-demo` foundation: 8 to add) | `/slipway:plan dev --layer foundation` |
| 3 | `/slipway:deploy` | `$0` app; `$1` tag; `$2` env; `--no-wait`, `--no-ticket` | latest green `<prefix>-<app>-ci` tag on the default branch; `github.cd_environment` | immutable semver only (hook + workflow check), the app's image must exist in the registry, one app per run (own workflow, module and state), plan published before the human approval, never approves or re-runs on its own | **implemented, exercised 2026-09-15** (`0.1.14 → dev`, run 35017963941) | `/slipway:deploy 0.1.14 dev` |
| 4 | `/slipway:verify` | `$0` app; `$1` env; `$2` tag; `--no-write`, `--json` | env = `github.cd_environment`; tag = latest deployed for that app | read-only against cloud and repo; never deploys, re-runs or edits app code; only writes `.slipway/evidence/<app>/<tag>.md`; CONFIRMED needs a positive observation | **implemented, exercised 2026-09-16** (`/slipway:verify dev 0.1.16` headless: script + sub-agent second opinion → 19/19 confirmed) | `/slipway:verify dev 0.1.14` |
| 5 | `/slipway:ticket` | `$0` story create\|set, subtask start\|review\|done, comment, show, done, sync (plus create/start/review for compatibility); `$1` key or canonical title; `--evidence`, `--title`, `--epic`, `--message` | project, epic, current story, issue types and transitions from `.slipway/config.yaml` | tracking never blocks delivery (`tracker: none` or an unreachable Jira → queued for `sync`); requires the Atlassian MCP grant for writes (never falls back to tokens), reads issues back before claiming anything, no duplicate creation, `done` refused while evidence has REFUTED claims, delete/manage groups stay disabled | **implemented, exercised 2026-09-16** (DEVOPS-5: create → start → review → done, read-back after every write) | `/slipway:ticket done DEVOPS-5 --evidence .slipway/evidence/0.1.16.md` |
