# Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-09-09 | Plugin name `adlc`, marketplace name `adlc-marketplace`, skills invoked as `/adlc:<skill>` (bootstrap, dockerize, plan, deploy, verify, ticket) | Plugin skills are namespaced by plugin name; prefixing skills with `adlc-` would read `/adlc:adlc-bootstrap`. Note: the official Anthropic marketplace once had a plugin named `adlc` (renamed `agentforce-adlc`, Salesforce). Names are scoped per marketplace, so no technical conflict, but the review packet should mention it. |
| 2026-09-09 | This repo versions itself with semantic-release; `adlc-demo` uses NBGV | Proves both versioning options on real repos without running two schemes on one repo. |
| 2026-09-09 | Option registry (`options.yaml`) with `implemented / planned / later` status is the single source of truth for the interview | User requirement: every "now vs later" choice is the end user's, the system supports the alternatives. |
| 2026-09-09 | Repository LICENSE not yet chosen | Owner's call; required before marketplace review (see SECURITY-LICENSE-CHECKLIST.md). |
| 2026-09-09 | Skill namespacing confirmed by a headless `claude -p --plugin-dir` run: model-invocable skills appear as `adlc:bootstrap`, `adlc:delivery-knowledge`; command skills (`disable-model-invocation: true`) are hidden from the model and user-invoked as `/adlc:<name>` | Spike result; matches docs. |
| 2026-09-09 | DHI .NET 8 tag availability **unverified**: `docker manifest inspect dhi.io/...` returns `unauthorized` anonymously; catalog pages list only recently pushed 10.x images. Fallback verified: `mcr.microsoft.com/dotnet/aspnet:8.0-noble-chiseled` and `mcr.microsoft.com/dotnet/sdk:8.0` exist | Owner to run `docker login dhi.io` and re-check; if 8.0 is absent, `base_image: mcr-chiseled` becomes the implemented .NET option and `dhi` stays for nginx/node. |
| 2026-09-09 | Schema validation uses ajv 2020-12 strict mode with `strictRequired` off | Conditional `required` inside `if/then` is valid JSON Schema; the strictRequired heuristic rejects it. |
| 2026-09-09 | **DHI verified for .NET 8** after `docker login dhi.io`: `dhi.io/dotnet:8.0-sdk` (build stage), `dhi.io/aspnetcore:8.0` and `:8.0-debian13` (runtime), `dhi.io/nginx:1.29`, `dhi.io/node:22`, `dhi.io/node:24-dev` exist; `dhi.io/nginx:latest` and `dotnet:8.0-dev` do not. `base_image: dhi` stays implemented; `mcr-chiseled` remains the planned fallback | Anonymous pulls from dhi.io are unauthorized; CI runners will need `docker login dhi.io` with a Docker Hub token stored as a GitHub secret. |
| 2026-09-09 | Repos live in **github.com/integranz** (`integranz/adlc` pushed, private). Azure subscription is the Integrant-owned "Ontellus Pay As You Go" (`2e8a6e6a-…`), confirmed usable by the owner | Owner decision. Private repo means GitHub Environments with required reviewers need a paid org plan; otherwise make `adlc-demo` public or gate CD with a typed `confirm` input. |
| 2026-09-09 | `azure.subscription_id` / `tenant_id` are optional in `.adlc/config.yaml`; tooling falls back to `ARM_*` / `AZURE_*` env vars and GitHub secrets | Keeps tenant identifiers out of committed config so a repo can be made public safely. |
| 2026-09-09 | "Integrant marketplace review" is not available yet. Review path for Q3: own marketplace (`integranz/adlc`) + submission to Anthropic's community marketplace (`claude-community`, catalog `anthropics/claude-plugins-community`; Console form platform.claude.com/plugins/submit; pipeline runs `claude plugin validate` + safety screening; approved plugins are SHA-pinned in the public catalog, so this repo must be public before submitting) + a colleague's clean-install test | Goal 8 wording accepts "review-ready with a dated changelog" if a review cannot complete in time. |
| 2026-09-09 | Removed `plugins/adlc/settings.json` (`env.ADLC_CONFIG`). Skills read `.adlc/config.yaml` at a fixed path | Docs: plugin `settings.json` currently supports only `agent` and `subagentStatusLine`; unknown keys are silently ignored. |
| 2026-09-09 | **Terraform apply gate = one-shot human approval token**, not a hook `ask` decision | Docs: `permissionDecision: "ask"` becomes `allow` in `-p` (headless) sessions, so a prompt is not a gate in Routines/cloud. `scripts/approve-apply.sh <planfile>` (human, separate terminal) writes `.adlc/approvals/<sha256>` with a 10-min TTL; the hook consumes it once. Agent attempts to run the approve script are blocked. |
| 2026-09-09 | Four hooks instead of three: added `guard-readonly-agents.sh` | Plugin agents ignore `hooks`/`mcpServers`/`permissionMode` frontmatter, so "verify never fixes" needs a plugin-wide hook keyed on `agent_type`. Goal 2 counts the first three as the planned guardrails; the fourth is supporting. |
| 2026-09-09 | Security-relevant hooks do not use the `if:` prefix filter | `if: "Bash(terraform apply*)"` matches command prefixes; `cd x && terraform apply` would bypass it. Scripts fast-path on the full command instead. |
| 2026-09-09 | GitHub MCP: remote `https://api.githubcopilot.com/mcp/x/actions` (actions toolset, OAuth); read-only variant `/x/actions/readonly` exists | Verified in github-mcp-server `docs/remote-server.md`. Trigger tool: `actions_run_trigger` with `method: run_workflow`, `workflow_id`, `ref`, `inputs`. Read-only enforcement for explore/verify is done by the hook, so one server suffices. |
| 2026-09-09 | `.adlc/approvals/` must be gitignored in every target repo (template) | Approval tokens are local, single-use artefacts. |
| 2026-09-09 | Live headless check passed: `claude -p --plugin-dir … --allowedTools "Bash(terraform *)"` asked to run `cd infra/foundation && terraform apply tfplan.dev` → tool call blocked with `adlc guard: BLOCKED`; agents listed as `adlc:explore`, `adlc:execute`, `adlc:verify` | The block message revealed that the hook resolved the plan file against the session `cwd`, ignoring the `cd … &&` prefix (would also weaken the infra/app check). Fixed: the guard now follows leading `cd`/`pushd` chains and `-chdir=`; 5 tests added (72 total). |
| 2026-09-09 | Plugin `.mcp.json`: `atlassian` (http, `https://mcp.atlassian.com/v2/mcp`, OAuth), `github` (http, `https://api.githubcopilot.com/mcp/x/actions`, OAuth), `azure` (stdio `npx -y @azure/mcp@latest server start --namespace acr --namespace containerapps --namespace keyvault --namespace group --namespace role --namespace subscription --namespace monitor --read-only`) | Azure namespaces verified by `npx @azure/mcp@latest tools list` (63 namespaces, 398 tools). ACR/ACA coverage is inventory-level (`acr registry list`, `acr registry repository list`, `containerapps list`); digest and revision checks use `az` CLI in the verify recipes. Read-only enforced server-side by `--read-only` and per-agent by the read-only hook. |
| 2026-09-09 | Release workflow failed twice: `conventional-changelog-conventionalcommits@10` requires `conventional-changelog-writer@9`, but `@semantic-release/release-notes-generator` bundles an older writer. Pinned the preset to `^8.0.0`; local `semantic-release --dry-run --no-ci` then produced 0.1.0 with correct notes | Version base: annotated tag `v0.0.0` on the initial commit so the first release is `0.1.0`, not semantic-release's default `1.0.0`. Repo default workflow token permission is `read`; the workflow requests `contents: write` explicitly and semantic-release reported "Allowed to push". |
| 2026-09-09 | Third release failure: `set-plugin-version.sh` read `process.argv[2]` but with `node -e` arguments start at `argv[1]`, so it tried to open "0.1.0". Fixed and covered by a CI self-test step | `semantic-release --dry-run` skips `prepare`, so exec hooks must be tested directly. |
| 2026-09-09 | **v0.1.0 released by semantic-release** (run 34374516819): tag, GitHub release with notes, `plugin.json` bumped to 0.1.0, `CHANGELOG.md` generated, release commit `chore(release): v0.1.0 [skip ci]` on `main` | Proves the `semantic-release` versioning option end to end on a real repo (goal 7 CI tier evidence; goal 8 dated changelog). `adlc-demo` will prove `nbgv`. Every `feat:`/`fix:` merged to `main` now cuts a release; `docs:`/`chore:` do not. |
| 2026-09-09 (day 3) | Scaffold engine is dependency-free Node: vendored `js-yaml` (MIT, single file) + a standalone validator compiled from `config.schema.json` with ajv (checked in, CI verifies it is fresh) + a ~120-line renderer | Target repos must not need `npm install` to run `/adlc:bootstrap`; Claude Code sessions (local, cloud, Routines) always have Node. Python/PyYAML is not guaranteed. |
| 2026-09-09 (day 3) | Template delimiters are `<% … %>` (`<%#each%>`, `<%#if%>`, `<%#eq%>`, `<%else%>`, `<%! %>`), not `{{ }}` | `{{ }}` collides with GitHub Actions `${{ }}` and Helm; `<% %>` collides with nothing we generate (YAML, HCL, Dockerfile, nginx, Markdown). |
| 2026-09-09 (day 3) | Scaffold is two-phase: render every template in memory, then write. Any template error or invalid/planned option exits 1 with nothing written | Rule from the plan: "never write a partial scaffold". Existing files are kept unless `--force`; `.gitignore` is merged line-wise. |
| 2026-09-09 (day 3) | Schema: `format: uuid/uri` replaced by regex patterns; `const`/`uniqueItems` replaced (duplicates checked in code) | Keeps the compiled validator free of the ajv runtime helpers (`ajv-formats`, `equal`). |
| 2026-09-09 (day 3) | Option registry gained `distribution:` (marketplace `adlc-marketplace`, repo `integranz/adlc`, plugin `adlc`); rendered into target repos' `.claude/settings.json` (`extraKnownMarketplaces` + `enabledPlugins`) | Single source for where the plugin comes from; cloud sessions auto-install from it, local teammates get the install hint. |
| 2026-09-09 (day 3) | Template layout `common/files`, `<dimension>/<option>/files`, `stack/<stack>/app` | Adding an option is additive: registry status + schema enum + template dir + knowledge reference. Dimensions with no repo-side files (tracker, registry, secret store) simply have no template dir. |
| 2026-09-13 (day 4) | `integranz/adlc-demo` has real apps: .NET 8 minimal API (`/health` + `/api/health` return `{status, version, startedAt}`, xunit tests via `WebApplicationFactory`, Kestrel on 8080, `IncludeSourceRevisionInInformationalVersion=false`) and React 19 + Vite 8 (`__APP_VERSION__` from `VITE_APP_VERSION`, vitest). `version.json` (NBGV 0.1, `main` public) → `nbgv get-version` = 0.1.2 after bootstrap commit | The version string must equal the image tag exactly for `/adlc:verify`; .NET's default `+<sha>` suffix would break that, so it is disabled and the commit lives in the OCI label. |
| 2026-09-13 (day 4) | **Bootstrap skill gap found by a headless test**: `/adlc:bootstrap --yes --no-ticket` on a repo without config scaffolded 12 files using `config.example.yaml` values as "defaults" (owner, Azure names, Jira site). Fixed by adding an explicit defaults table to the skill (identifiers and tracker details have **no default**; the example file is a shape reference only) and re-testing: the skill now stops before Step 3, lists the missing values, writes nothing | Rule: every skill with a non-interactive path gets a headless negative test in a scratch clone (`git status --porcelain` must be empty). |
| 2026-09-13 (day 4) | First real `/adlc:bootstrap` on `adlc-demo` (interactive interview: westeurope, `rg-adlc-demo-dev`/`acradlcdemo`/`kv-adlc-demo-dev`/`id-adlc-demo-dev`, state `rg-adlc-tfstate`/`stadlctfstate`, Jira `DEVOPS` on integranz.atlassian.net, all nine options confirmed) → `.adlc/config.yaml` + 11 generated files + `.gitignore` merge, 14/14 verification claims confirmed, commit `28d10b3` | Names were checked available with `az … check-name` before proposing them. Subscription/tenant ids kept out of the public repo. |
| 2026-09-13 (day 4) | Explore agent found the `/api/health` vs `/health` mismatch in the demo before any container existed | Evidence that the exploration role adds value beyond inventory: it reads the code paths, not just file names. |
| 2026-09-13 (day 4) | Azure MCP server (`npx -y @azure/mcp@latest`, 3.0.0-beta.43) answers the MCP `initialize` handshake correctly with the read-only namespace set; warm start ≈ 2.1s. The headless bootstrap run reported it as "failed to connect" once, consistent with a **cold `npx` download exceeding Claude Code's MCP startup timeout** on a fresh machine or cloud session | Mitigations to document in MCP-INTEGRATION.md: pre-warm with `npx -y @azure/mcp@latest --version` in the cloud environment setup script / first local session, or raise `MCP_TIMEOUT`. Pinning an exact version is deferred until the server leaves beta. |
| 2026-09-13 | First `--apply` of `setup-azure.sh` on the Integrant subscription created the app registration, SP, both federated credentials, both resource groups, the state storage account and the human's Storage Blob Data Contributor, then failed on `roleAssignments/write` for "User Access Administrator on rg-adlc-demo-dev": the user's UAA at subscription scope carries an ABAC condition forbidding assignment of Owner / UAA / RBAC Administrator, and the existence check ignored inherited roles. Fixed: `--include-inherited` checks, no privileged-role assignments ever, human assignments soft, preflight prints the condition's forbidden roles | Least-privilege design confirmed by the incident: CI/CD needs only Contributor + Storage Blob Data Contributor, both allowed under the condition. |
| 2026-09-14 (day 5) | **DHI image facts** (pulled and inspected): `dhi.io/aspnetcore:8.0` runs as uid 65532, no shell, `ASPNETCORE_HTTP_PORTS=8080`, no ENTRYPOINT; `dhi.io/dotnet:8.0-sdk` and `dhi.io/node:22-dev` are root with a shell (build stages only); `dhi.io/nginx:1.29` runs as `nginx` (65532), listens on 8080, includes `conf.d/*.conf`, owns `/run/nginx`, `/var/cache/nginx`, `/var/log/nginx`. Anonymous pulls are refused | Templates: no `RUN` in runtime stages, no `HEALTHCHECK` (no curl), no envsubst; config baked at scaffold time. |
| 2026-09-14 (day 5) | **Frontend → API routing**: Container Apps resolves `http://<app-name>` (port 80) through its Envoy proxy and routes by the `Host` header (learn.microsoft.com/azure/container-apps/connect-apps, 2026-03). nginx therefore uses `proxy_pass http://api;` and must **not** override `Host` (default `$proxy_host`). Locally the container port differs, so a second `nginx.local.conf` (`http://api:8080`) is mounted by `compose.yaml` | One image for cloud and local; the only difference is a mounted file. Requires compute modules to name Container Apps exactly `<app.name>`. |
| 2026-09-14 (day 5) | Per-app `build.*` block in the config schema (`project`, `assembly`, `dist_dir`, `node_version`, `dotnet_version`, `nginx_version`) with deterministic detection of the single non-test `.csproj`; `scaffold.cjs --app <name>` renders one app's stack templates; build errors fail only when stack templates are in scope | Keeps `.adlc/config.yaml` the single source while avoiding hand-written Dockerfile inputs. |
| 2026-09-14 (day 5) | Verified on `adlc-demo` at 0.1.6: api 57 MB, web 11 MB, both distroless and uid 65532, labels correct, `docker compose` web→nginx→api returned the API version. Headless `/adlc:dockerize api` run: execute built, verify checked 9 claims (8 CONFIRMED, `docker top` UNVERIFIABLE because the permission harness blocked it → added to the skill's allowed tools) | First real use of the **execute** and **verify** profiles on real work (goal 6). Vite emits the version as a template literal, so the bundle check matches the bare string. |
| 2026-09-14 (day 5) | Tooling incident: a heredoc whose terminator was not alone on its line swallowed the rest of a build script (no error, half the work silently skipped); a `docker top -o` flag aborted a `set -e` script before `compose down` | Lessons for skills and CI steps: terminators on their own line, `set -eo pipefail` plus explicit exit-code checks, always tear down in a trap. |
| 2026-09-14 (day 6) | **azurerm provider 5.x** (5.5.0, released 2026-09-10) adopted for the foundation layer: `rbac_authorization_enabled` is required, `resource_provider_registrations = "none"` is set explicitly (providers are registered once by an owner; the CI/CD principal never needs that right). Backend `azurerm` with `use_azuread_auth = true` (no storage keys). `principal_type` set on every role assignment because the assigner's ABAC condition may filter on it; `skip_service_principal_aad_check` for the CI/CD principal | Verified in the 5.0 upgrade guide and resource docs; `terraform init -backend=false && validate` runs in the scaffold test suite and CI (`hashicorp/setup-terraform`). |
| 2026-09-14 (day 6) | Ownership split: `setup-azure.sh` owns the resource group and state storage; `infra/foundation` owns Log Analytics, ACR, the apps' identity, Key Vault and all role assignments; `infra/app` owns compute and image tags | Terraform never creates role assignments from CI, and the foundation is applied by a human behind the approval token. |
| 2026-09-14 (day 6) | First real plan on `adlc-demo`: init against the Entra-authenticated backend, fmt ok, validate ok, **8 to add / 0 change / 0 destroy** (`tfplan.dev` kept locally, gitignored). Apply left to the human through `approve-apply.sh` | Evidence for goal 3 (`/adlc:plan` exercised) and the setup for the goal 2 live gate test. |
| 2026-09-14 (day 6) | `terraform fmt -check` in the tests caught misaligned `=` in a rendered `locals.tf`; the template is now generated from `terraform fmt` output | Alignment depends only on key names, so a formatted template stays formatted for any config values. |
| 2026-09-14 (day 6) | **Foundation applied through the live gate** (user, Claude session with `--plugin-dir`): first `terraform apply` blocked by the hook (no token), human ran `approve-apply.sh`, second apply allowed and token consumed, a further plan shows no changes; ACR `acradlcdemo`, KV `kv-adlc-demo-dev`, identity `id-adlc-demo-dev` (client 9d6f954c-…), Log Analytics `log-adlc-demo-dev` and 4 role assignments verified with `az`; state blob `adlc-demo/foundation/dev.tfstate` present | Goal 2 live evidence: the guardrail works end to end with a real approval. |
| 2026-09-14 (day 6) | Hook defect found in that run: `terraform apply tfplan.dev \| less` was blocked with "plan file 'less' does not exist" because the parser took the last token of the whole command. Fixed: plan-file extraction stops at `\|`, `;`, `&&`, `\|\|`, `>`, `<`; 4 new test cases (76 total) | Fail-closed behaviour was correct, the message was misleading; parsing now follows the shell grammar it claims to check. |
| 2026-09-14 (day 6) | Observation: with `claude --plugin-dir`, hooks run from the local checkout, not from the marketplace cache. Expected (`--plugin-dir` takes precedence for the session); the clean-install test in week 3 runs without `--plugin-dir` to prove the marketplace copy | Documented in CLEAN-INSTALL-TEST.md plan. |
| 2026-09-15 (day 7) | **First CI run on `adlc-demo` failed at `azure/login`**: `AADSTS700213 No matching federated identity record found for presented assertion subject 'repo:integranz@326982263/adlc-demo@1362809496:ref:refs/heads/main'`. Cause: GitHub's *immutable OIDC subject claims* are enabled on the repository by default (`GET /repos/{owner}/{repo}/actions/oidc/customization/sub` → `use_immutable_subject: true`, `sub_claim_prefix: repo:<owner>@<owner-id>/<repo>@<repo-id>`; documented in the REST reference: "When true, OIDC tokens will use a stable, repository-ID-based sub claim"). Our federated credentials used the classic `repo:integranz/adlc-demo:…` subject | Fix: `setup-azure.sh` now reads the prefix from that endpoint (or derives it from the repository and owner ids when `gh` is absent) and creates the credentials with the exact value; the classic-form credentials stay (harmless). Lesson recorded in `identity-and-secrets.md`: never assume the subject format, read it from GitHub. |
| 2026-09-15 (day 7) | Second CI defect from the same run, fixed first: web tests failed with `vitest: not found` because the workflow ran `test_command` without installing Node dependencies. Template now runs `npm ci --prefix <app>` per Node app with an npm cache keyed on each lockfile | `test_command` in config is what a developer runs; CI templates own the environment setup per stack. |
| 2026-09-15 (day 7) | The setup script's preflight refused to run while the `az` CLI was logged in as a different identity (Ontellus tenant): `allowedToCreateApps=false`, no roles. Correct fail-closed behaviour; the fix is `az account set` back to the Integrant subscription | Consider a preflight check that the configured resource group is visible in the active subscription. |
| 2026-09-15 (day 7) | **First successful CI release on `adlc-demo`** (run 34997898117, re-run of the failed jobs after the immutable-subject credentials were added): tests green, images pushed as `acradlcdemo.azurecr.io/adlc-demo/{api,web}:0.1.12` and `:sha-6032db2022`, single-manifest digests (provenance off), `release-manifest-0.1.12` artifact, git tag `v0.1.12` created by `nbgv tag` | Goal 7 "CI tier: adopted and proven"; the deploy step consumes the manifest. NBGV's `GitCommitIdShort` is 10 characters. |
| 2026-09-15 (day 8) | **First deployment through the gated CD**: `/adlc:deploy 0.1.14 dev` (headless, real plugin run; `gh` fallback because the GitHub MCP has no OAuth grant headless) dispatched `cd.yml`; the `plan` job published "3 to add" and the plan artifact; the user reviewed the downloaded artifact and approved the `dev` environment; `apply` created `cae-adlc-demo-dev`, `api` and `web`; smoke test CONFIRMED both health URLs with `version == 0.1.14`. Independent verification: HTTPS, web→`http://api` proxy through the Container Apps Envoy, bundle version, HTTP→HTTPS 301, `plan -detailed-exitcode` = 0, revisions Healthy at 100 %. Evidence: `adlc-demo/.adlc/evidence/0.1.14.md` | Goal 7 CD row proven; goals 3 (`deploy`) and 5 (GitHub read path) exercised. Improvement logged: deploy by digest (from the release manifest) so the running-image check becomes direct. |
| 2026-09-15 (day 8) | Container Apps facts applied in the template: apps named after `apps[*].name` for `http://<app>` discovery, `logs_destination = "log-analytics"`, HTTP probes on `health_path`, 0.25 vCPU / 0.5Gi, min 1 replica for verification, `depends_on` upstreams, Key Vault secret references via the identity (`apps[*].secrets`) | azurerm 5.x `azurerm_container_app` / `azurerm_container_app_environment` docs; `terraform validate` runs in the test suite. |
| 2026-09-15 (day 8) | CD is two jobs: `plan` (no environment, publishes summary + plan artifact) then `apply` (environment-gated, applies the exact plan file, smoke test, evidence artifact) | The reviewer sees the plan before approving; the apply cannot drift from what was reviewed. |
| 2026-09-15 (day 8) | CI now refuses to overwrite an existing registry tag and creates the release tag idempotently | Yesterday a manual re-dispatch on the same commit silently re-pushed `0.1.13`. |
| 2026-09-15/16 (day 9) | **`scripts/verify.cjs`** makes post-deployment verification deterministic: one row per claim with the literal command and comparison (CD run + human approval + evidence artifact, `image_tag` output, health `version == tag`, bundle version, upstream proxy, HTTP→HTTPS, active revision image/health/traffic, registry digest, `plan -detailed-exitcode`, repo hygiene). First run on `dev 0.1.14`: 19/19 CONFIRMED after one fix (below) | The verify sub-agent gives a second opinion only on rows that are not CONFIRMED; the skill never repairs. |
| 2026-09-16 (day 9) | `az acr manifest show … --query digest -o tsv` prints **nothing** in az 2.75 (the digest lives under `config.digest`), so the CD "image exists" step passed on exit code with a blank digest and the script marked the registry claim REFUTED. All digest lookups now use `az acr repository show -n <acr> --image <repo>:<tag> --query digest -o tsv` | Prefer commands whose output is asserted, not just their exit code. |
| 2026-09-16 (day 9) | Headless `/adlc:verify` in an **untrusted** scratch workspace: the permission gate refused `node`, `curl`, `terraform`, `gh api`, and the verify sub-agent's Bash; the parent still re-derived 13 claims from job logs and `az` and reported the rest UNVERIFIABLE instead of guessing. Fixes: skill grants now list both quoted and unquoted `${CLAUDE_PLUGIN_ROOT}` script paths and the exact read-only commands; repo `.claude/settings.json` allow-list applies once the folder is trusted (cloud sessions and Routines are trusted by definition) | Headless negative tests must run in a trusted folder to be representative; recorded in the tier matrix runbooks. |
| 2026-09-16 (day 9) | Terraform `tags.generator` no longer embeds the plugin version: every plugin release re-rendered `locals.tf` and produced three in-place tag updates (false drift) | Generated files must be stable across plugin versions unless behaviour changes. |
| 2026-09-16 (day 9) | Second deploy→verify cycle: `/adlc:deploy 0.1.16 dev` (headless skill run) → human approval → apply; `/adlc:verify dev 0.1.16` (headless skill run, script executed this time in the trusted repo folder) → 18/19, the verify sub-agent's second opinion identified the one REFUTED row as an **outputs-only refresh** (`latest_revision` names read back before Container Apps switched revisions). Verifier refined: drift = resource changes (`terraform show -json` on the saved plan), outputs-only refresh is CONFIRMED with a note; the verifier's own evidence files are excluded from the clean-tree claim → **19/19** for 0.1.16 | Evidence `adlc-demo/.adlc/evidence/0.1.16.md`. Goal 6: the verify role added value (correct diagnosis) without touching anything. |
| 2026-09-16 (day 9) | Jira integration named explicitly as the **Atlassian Rovo MCP Server** (same product as the former "Remote MCP Server", `https://mcp.atlassian.com/v2/mcp`, OAuth 2.1, consumes Rovo credits) | User request for clarity; wording aligned in `.mcp.json` docs, the ticket skill, options registry and references. |
| 2026-09-16 (day 9) | **MCP golden path done** (goal 5): in an interactive plugin session in `adlc-demo`, the user authorised the Atlassian Rovo MCP Server and ran `/adlc:ticket create/start/review/done` on `integranz.atlassian.net` → **DEVOPS-5**, each write followed by a read-back; `done` was permitted only after the skill re-read `.adlc/evidence/0.1.16.md` and found no REFUTED claim; the transition was chosen by target status name (id 41) | First OAuth attempt failed with "Your account isn't associated with a supported site" (Atlassian-side account/site association); documented in MCP-INTEGRATION.md. All five command skills are now exercised on real work. |
| 2026-09-16 (day 10) | **Rules audit** (goal 4) completed for both repos and the user scope: 9 overlaps listed, 3 fixed in templates/repo (`branching.md` no longer loads on every `*.md`; ticket-path non-negotiable removes the personal Jira skill trigger overlap; `precedence.md` added to the plugin repo), 3 accepted as intentional layering, 3 user-scope recommendations (delete duplicate legacy commands, add frontmatter to `infra-ticket`, remove Cursor copies of gitnexus skills) | `docs/RULES-AUDIT.md`; both repos sign off at 100 %. |
| 2026-09-16 (day 10) | **License MIT** (copyright Abdelazim Ali, corrected from "Integrant" on the owner's instruction) at repo root and in the plugin; `plugin.json` gets `license`, `repository`, `homepage`; security checklist completed except the second-person review of the MCP policy; `shellcheck` added to CI; `npm audit --omit=dev` = 0 (no runtime deps) | Owner decision. |
| 2026-09-16 (day 10) | **Tier decisions**: Claude Routine = dated skip (not allowed on the owner's subscription); Cursor Automations = adopted (team seat active), runbook `docs/CURSOR-AUTOMATION.md`; Claude Code on the web = to confirm; cloud environment setup script template added (`.adlc/cloud-setup.sh`) | Goal 7 rows: CI proven, Routine decided, Cursor and cloud agents pending their run/decision. |
| 2026-09-16 (day 10) | **Cloud agents tier = dated skip**: Claude Code on the web is disabled by the organisation admin on the owner's account (claude.ai/code/disabled). With Claude Routines also unavailable, the non-interactive tiers in use are **CI** (proven) and **Cursor Automations** (adopted, proven run pending); both Claude cloud tiers are recorded as explicit, dated out-of-scope decisions with the plugin kept cloud-ready | Goal 7 counts a dated decision as a complete row. Consequence for goal 1: the independent run of the `bootstrap` skill must be done by a second person locally (clean `CLAUDE_CONFIG_DIR`), not in a cloud session. |
| 2026-09-16 (day 10) | **Cursor Automation proven** (goal 7, last row): `adlc-demo PR review` reviewed PR #2 (CONTRIBUTING.md) 60 s after opening with the correct verdict and the requested three-section format; it posts a PR *review*, not a comment | Goal 7 rows: CI proven, Cursor proven, Routine and cloud agents dated skips → 4/4 complete. |
| 2026-09-16 (day 10, change request) | **Plugin renamed `adlc` → `slipway`**, marketplace `adlc-marketplace` → `slipway-marketplace`, repo `integranz/adlc` → `integranz/slipway`, config directory `.adlc/` → `.slipway/`, skill namespace `/adlc:*` → `/slipway:*`, agents `slipway:explore|execute|verify`, env `SLIPWAY_APPROVAL_TTL`. **ADLC stays the name of the lifecycle/methodology** (Teamflect goal, study plan); slipway is the tool that implements it. Demo repo renamed to `integranz/slipway-demo`; its `project.name` and all Azure resource names stay `adlc-demo`-based (immutable identifiers). History (`CHANGELOG.md`, earlier rows of this log) is not rewritten | Owner's change request. Drivers: a plugin named `adlc` already existed in the official marketplace (renamed `agentforce-adlc`), and a distinct product name is needed before the community submission. Consequences: consumers re-add the marketplace (`/plugin marketplace add integranz/slipway`, `/plugin install slipway@slipway-marketplace`); the demo's OIDC subjects change with the repo name → `setup-azure.sh --apply` again; `generator` tags change to `slipway` → one in-place tag update on the next applies. Shipped as 0.12.0; 1.0.0 planned at the freeze. **Post-rename fixes (same day):** (1) the rename's word-boundary rule also rewrote the real state resource group `rg-adlc-tfstate` to `rg-slipway-tfstate` in the demo config and its rendered backend blocks; restored before any deploy ran. Lesson: protect every identifier that names a real cloud or GitHub object, not only the project id. (2) GitHub's immutable OIDC subject embeds the repository name, so the rename invalidated both federated credentials; `setup-azure.sh` now updates a same-name credential in place instead of failing on `already exists`. |
| 2026-09-17 (day 11, change request) | **One CI and one CD per app** (plugin 0.13.0), modelled on an internal Azure Pipelines monorepo and improved on it: workflow names `<repo>-<app>-ci|cd`, thin callers of shared reusable workflows `_ci.yml`/`_cd.yml`; path-filtered triggers rendered from `.slipway/config.yaml` (single source of truth) and equal to the app's `version.json` `pathFilters` (`verify` refutes a mismatch); one `version.json` per app with `release.tagName: <app>/v{version}` instead of a prerelease suffix; .NET `ProjectReference`s outside the app are detected and become inputs (repository-root build context, `Dockerfile.dockerignore`); shared paths trigger and version every app that lists them; options `cd_trigger` (`manual`/`on-ci-success` via `workflow_run` + release manifest, human gate kept) and `pr_checks` (`path-filtered`/`always-run-gate`, because a path-skipped workflow leaves required checks Pending); Terraform split into `infra/apps/<app>` with its own state, Container Apps environment moved to `infra/foundation`. semantic-release stays implemented for single-app repos and is `planned` for monorepos. The combined layout (`ci.yml`, `cd.yml`, `infra/app`, root `version.json`) is superseded; the scaffold reports legacy files and never deletes them. |
| 2026-09-17 (day 11) | **Hook coverage finding**: the background job that executed the per-app cutover had no slipway hooks loaded (started from `adlc-poc`, which has no project settings enabling the plugin; user scope enables only another plugin; an old `--plugin-dir` path had moved). Process discipline held (every apply was human-approved), enforcement did not. Decision: install the plugin at user scope on agent machines, probe the guard before applies (`echo approve-apply-probe` must be blocked), check the approvals directory is empty afterwards; documented in HOOKS.md. |
| 2026-09-17 (day 11, review with the owner) | **Tracking model**: epic (optional, asked in the interview) → one Story per delivery (onboarding, then one per change request named in `jira.story_key`) → one Subtask per unit of work (bootstrap, dockerize per app, foundation per env, deploy per app/tag/env; verify comments on the deploy subtask). Skills keep subtasks current at each step; the story auto-closes when every subtask is done. **Tracking is never a blocker**: new `tracker: none` option, and an unreachable Jira queues updates in a gitignored `.slipway/tracking-queue.jsonl` replayed by `/slipway:ticket sync`. Facts: Jira Cloud uses the `parent` field for subtask → parent and story → epic (Epic Link deprecated); subtask type name differs by project type. Open check: `createJiraIssue` parent parameter in the Rovo MCP tool (edit fallback implemented). Plugin 0.14.0. |
| 2026-09-20 (day 12) | **Tracking golden path done** (DEVOPS-6 → DEVOPS-7/8, auto-closed). Findings folded into 0.14.3: the story follows its subtasks (To Do → In Progress on the first `subtask start`); auto-close is mandatory output after every `subtask done`; `createJiraIssue` takes `parent` and `issueType` directly (the reference's open check is settled); the Rovo server has 8 primary Jira tools plus `discover`/`executeRead`/`executeWrite` meta-tools; the verify hygiene claim now refutes only on uncommitted state/plan/tfvars/env/key files (an uncommitted config edit had pushed a deploy subtask to In Review for no delivery reason). |
| 2026-09-20 (day 12, end-user review with the owner) | **One command, in-session approvals, secret seeding, GitHub automation** (plugin 0.15.0): `/slipway:launch` runs preflight → bootstrap → Azure prerequisites → GitHub secrets/environment/ruleset → images → foundation → CI/CD → verify → tracking, resumable. Hooks: a `terraform apply` without a token now returns `ask` in an attended session (plan summary as the reason) and deny otherwise; new `guard-admin-actions.sh` forces prompts for `setup-azure.sh --apply`, GitHub environment/ruleset/secret writes and deployment approvals, denies them unattended, and keeps the seed file `.slipway/.env` and secret variables out of the transcript. `cd_approval: in-session` approves pending deployments through the GitHub API under the human's account after an explicit answer. **Correction**: hook `ask` does not become `allow` headless (measured in `claude -p`: the call is refused with the reason); the token stays for unattended runs only. Attendance is read from the undocumented `CLAUDE_CODE_SESSION_ATTENDED` (1 in the owner's terminal, 0 in `-p`/background), failing safe when absent. |
| 2026-09-21 (day 13) | **Launch golden path done** by the owner in an attended session (plugin 0.15.2): `/slipway:launch` found the demo delivered, created the four subtasks that predated the tracking model (Bootstrap, Dockerize ×2, Foundation) under DEVOPS-6 and closed them, re-verified api and web (PR #11), nothing waiting. `cd_approval: in-session` proven: run 35607330326 approved through the GitHub API under the owner's account with the plan line in the comment; apply no-op, smoke confirmed. Operational finding: the demo also had a **project-scope** plugin install (from its `.claude/settings.json`) lagging at 0.14.2 next to user scope 0.15.2; both must be updated after a release (`claude plugin update … --scope project` inside the repo). Documented in README. |
| 2026-09-21 (day 13, stack-agnostic core, Option A for the freeze) | **Any stack, any structure** in 0.16.0: `stack: custom` (bring your own Dockerfile, contract verified), apps at the repository root (`path: .`, root filters excluding `.slipway` and `*.md`, shadowing ignore file, named after the role → `taskflow-api-ci`), per-app `test_services`/`test_env` rendered into the caller (tests moved out of `_ci.yml`). Decided with the owner: non-secret settings stay environment variables (`config_store: env`; App Configuration later), database provisioning is a `planned` option in the foundation layer with the connection string written to Key Vault (asked only when a database dependency is detected), code changes to apps are proposed and land through confirmed pull requests. Deferred to 1.1 after the 28 Sep submission: curated `node-ts-api`, the configuration scanner, the database option. First target: `integranz/taskflow` (TypeScript API at the root, PostgreSQL). |
| 2026-09-21 (day 13) | **Re-run tolerance for the immutable-tag guard** (0.16.1): the api CI run for 0.2.5 pushed the image and then failed in `actions/upload-artifact` with a GitHub-side `403 Forbidden` on finalize (transient); a re-run would have been refused by the guard because the tag now existed. The guard now distinguishes a first attempt (existing tag → fail, immutability) from a re-run of the same run (`github.run_attempt > 1` → reuse the digest, skip build and push). Orphan: `adlc-demo/api:0.2.5` exists without a git tag or deployment; harmless, superseded by 0.2.6. |
| 2026-09-22 (day 14, freeze) | **Contents frozen as 1.0.0** on the owner's instruction (Option A): one CI and one CD per app with path filters and per-app versions; story → subtask tracking with an offline queue; `/slipway:launch` with in-session approvals (attended sessions) and the token gate for unattended ones; secret seeding without transcript exposure; `stack: custom`, apps at the repository root, per-app CI service containers; five guard hooks (113 tests), six commands, three sub-agents, three MCP servers. Cut by a `BREAKING CHANGE` footer on the freeze commit, the only way semantic-release moves from 0.x to 1.0.0; nothing in the contract breaks for consumers of 0.16.1. From here: fixes as 1.0.x, the clean-install test, the independent bootstrap run and the MCP second-person review (25 Sep), the review packet and the community marketplace submission (28 Sep). Planned for 1.1: curated `node-ts-api`, configuration scanner, database provisioning, App Configuration. |
| 2026-09-22 (day 14) | **Plugin repository protected and channelled**: ruleset on `main` (pull requests only, `validate` check required, no force push or deletion) with the `slipway-release` GitHub App (id 5027028, Contents read/write, installed on this repository only) as the single bypass actor; the release workflow mints its token with `actions/create-github-app-token` because deploy keys are disabled for the organisation and GitHub's Actions token cannot bypass rulesets. Proven: 1.0.1 released by `slipway-release[bot]`, a direct push refused with GH013. `next` branch publishes `1.1.0-next.N` pre-releases for 1.1 work; `main` takes fixes only until the review. Every change here, including docs, now travels through a pull request. |
| 2026-09-22 (day 14, change request) | **slipway is a Cursor plugin too** (owner: "we need a cursor plugin with claude code, not just Cursor Cloud Agents"; the 25 Sep clean-install test and the taskflow demo run in Cursor). Dual-format repository, one plugin directory: `.cursor-plugin/marketplace.json` (`pluginRoot: plugins`) next to `.claude-plugin/marketplace.json`; `plugins/slipway/.cursor-plugin/plugin.json` next to `.claude-plugin/plugin.json` (both versioned by the release); `skills/` shared unchanged (Agent Skills format); `cursor/hooks.json` + adapters feed Cursor hook input to the unchanged guard scripts; `cursor/agents/*.md` and `cursor/mcp.json` are **generated** from `agents/*.md` and `.mcp.json` (`npm run build:cursor`, CI fails when stale); `cursor/rules/slipway.mdc` (always on) maps `${CLAUDE_PLUGIN_ROOT}`, `AskUserQuestion` and `Agent` onto Cursor and names the guardrails; a `sessionStart` hook tells the agent the plugin root. Grounded on cursor.com/docs (plugins, reference/plugins, hooks, reference/hooks, reference/third-party-hooks, subagents, skills) and the `fieldsphere/cursor-team-marketplace-template` layout. Evidence that Cursor already imports this repository: `~/.cursor/plugins/cache/integranz-slipway/slipway/<sha>` holds the plugin at 0.13.2 from the owner's 17 Sep import, so a marketplace refresh picks the new manifests up | One source for skills and guards, two hosts; no copy of skills into `.cursor/skills` per repository (the `cursor_mirror` option stays reserved) |
| 2026-09-22 (day 14) | **Approval policy in Cursor**: a local foundation `terraform apply` always needs the human token (Cursor documents the hook `ask` decision as not enforced for `preToolUse`, nothing for the other events, and fails open on hook crashes unless `failClosed: true`); administrative actions return `ask` and otherwise rely on Cursor's own command approval; explore/verify are read-only via `readonly: true`; every adapter failure is an explicit deny. Recorded in `docs/HOOKS.md` §6 with the list of behaviours still to verify live on 25 Sep | "Never apply without a human" must hold mechanically whatever the user's auto-run setting; Cursor cannot give that guarantee through a prompt |
| 2026-09-22 (day 14) | **Freeze and version**: the Cursor plugin is a new capability, so the merge is a `feat(cursor)` → **1.1.0** on `main` rather than a `1.1.0-next.N` pre-release, because Cursor's marketplace import and the local install follow `main` and the review test itself runs in Cursor. What 1.0 delivers is unchanged; the owner decides by merging the pull request | The review line must be installable where the review happens |
| 2026-09-22 (day 14, live test by the owner) | **Cursor 3.21.16 loads no plugin hooks**: the probe ran in the owner's Cursor session although the plugin was installed. Cause (Cursor build): plugin hooks load only when Claude Code hooks are enabled and the `enable_cc_plugin_import` feature gate is on; the team admin has third-party plugin import disabled. Also observed: the shell tool arrives as a generic `preToolUse` event with `cwd: ""`. Fix (1.1.1): the install script registers the adapters in `~/.cursor/hooks.json` (merged, uninstallable); every slipway repository gets `.cursor/hooks.json` + `.slipway/cursor-hooks.sh` (shim that finds the installed plugin, allows with a session-start warning without it); `pretooluse.sh` dispatches by tool name; a 3-second decision cache makes double delivery of one tool call harmless (the apply token is consumed once); the attended marker defaults to 1 in Cursor. Plugin-level `cursor/hooks.json` stays for builds that load it | The guards must be active in the host where the review test runs; a plugin that only *shows* is not a guard |
