# Non-interactive tiers (goal 7)

| Tier | Decision | Trigger | Scope | Secrets / MCP policy | Review path | Monitoring | Rollback | Proven run |
|---|---|---|---|---|---|---|---|---|
| **CI (GitHub Actions)** | adopted | `push` to `main`, `pull_request`, `workflow_dispatch` | `integranz/slipway-demo`; PR = build+test only, `main` = build, test, version, push, release | OIDC federated credentials (no cloud secret); `DOCKERHUB_TOKEN` + `DOCKERHUB_USERNAME` for base-image pulls; no MCP | PR review before merge; release job summary lists tags and digests | Actions run status, job summaries, `release-manifest-<v>` artifact; `gh run list/view` | images are immutable, so rollback = deploy the previous tag with `/slipway:deploy` | **2026-09-15, run 34997898117**: 0.1.12 pushed to `acradlcdemo`, tag `v0.1.12` |
| **CD (GitHub Actions, environment-gated)** | adopted | `workflow_dispatch` (tag, environment) from `/slipway:deploy <app>`, or `workflow_run` after the app's CI with `cd_trigger=on-ci-success` | `infra/apps/<app>` Terraform apply in the `dev` environment | OIDC (`environment:dev` subject); required reviewer on the environment; deployments only from `main` | human approval in the GitHub environment before apply | run status, `/slipway:verify` evidence file | redeploy previous tag with `/slipway:deploy <previous tag> <env>` (revisions kept) | **2026-09-15, run 35017963941**: 0.1.14 deployed to `dev` after approval by Zima-Zema; smoke and independent verification in `adlc-demo/.slipway/evidence/0.1.14.md` |
| **Claude Routine** | **not adopted — dated decision 2026-09-16** | — | — | — | — | — | — | Routines are not allowed on the owner's Claude subscription (owner statement 2026-09-16; the feature is a research preview whose availability is plan- and org-controlled). The scheduled/event-driven tier is covered by **Cursor Automations** instead. Revisit if the org enables Routines. |
| **Cloud agents (Claude Code on the web)** | **not adopted — dated decision 2026-09-16** | — | — | — | — | — | — | claude.ai/code shows "Disabled by org admin" for the owner's organisation (https://claude.ai/code/disabled, checked 2026-09-16). The plugin is cloud-ready regardless: repo `.claude/settings.json` declares the marketplace and plugin (auto-installed in cloud sessions) and `.slipway/cloud-setup.sh` installs the toolchain; revisit when the organisation enables it. |
| **Cursor Automations** | **adopted** (2026-09-16; Integrant team seat active) | Pull request opened / pushed on `integranz/slipway-demo` | rule-based PR review against AGENTS.md, CLAUDE.md and `.claude/rules/*` (runbook: `docs/CURSOR-AUTOMATION.md`) | no secrets, no MCP; repository content only | one PR comment, human decides; never approves or edits | cursor.com/automations run history + the PR comment | deactivate the automation | **2026-09-16, PR integranz/slipway-demo#2** (`docs: contributing guide`): review posted by `cursor[bot]` at 16:00:19Z, 60 s after the PR opened — https://github.com/integranz/slipway-demo/pull/2#pullrequestreview-5225124070 (automation `1f4725db-…`, run `bc-bbacbb51-…`). Verdict "No rule violations", correctly identifying the diff as hand-editable documentation and cross-checking the compose ports; no inline noise. |
